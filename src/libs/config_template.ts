const CONFIG = {
    HOST: "http://localhost",
    PORT: 4200,
    SMPT: {
        HOST: "smtp.gmail.com",
        PORT: 465,
        USER: "login",
        PASSWORD: "password"
    }
}

export default CONFIG